<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Color when switch is checked (semi-transparent version of thumb color) -->
    <item android:color="@color/purple_500" android:state_checked="true" android:alpha="0.5" />
    <!-- Default color for unchecked state (a very light gray) -->
    <item android:color="#E0E0E0" />
</selector>
    